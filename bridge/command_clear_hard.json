{
  "sqf": "private _fobPos = missionNamespace getVariable ['CLAUDE_FOB', getMarkerPos 'FOB RAVEN']; if (_fobPos isEqualTo [0,0,0]) then { { if ((toLower (markerText _x)) find 'raven' >= 0 || (toLower _x) find 'raven' >= 0) exitWith { _fobPos = getMarkerPos _x; }; } forEach allMapMarkers; }; ['CLEARING FOB PERIMETER - 500m RADIUS'] remoteExec ['systemChat', 0]; private _deleted = 0; { if ((side _x == EAST || side _x == OPFOR) && alive _x && _x distance _fobPos < 500) then { deleteVehicle _x; _deleted = _deleted + 1; }; } forEach allUnits; { private _veh = _x; if (_veh distance _fobPos < 500) then { private _crew = crew _veh; private _isEnemy = false; { if (side _x == EAST || side _x == OPFOR) then { _isEnemy = true; }; } forEach _crew; if (_isEnemy || count _crew == 0) then { { deleteVehicle _x; } forEach _crew; deleteVehicle _veh; _deleted = _deleted + 1; }; }; } forEach vehicles; [format ['PERIMETER SECURED - %1 threats neutralized', _deleted]] remoteExec ['systemChat', 0]; ['FOB RAVEN is now a SAFE ZONE.'] remoteExec ['systemChat', 0];",
  "description": "Hard clear 500m around FOB"
}
