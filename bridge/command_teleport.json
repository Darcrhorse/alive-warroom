{
  "sqf": "private _airfields = nearestLocations [[worldSize/2, worldSize/2, 0], ['Airport'], 50000]; if (count _airfields == 0) then { _airfields = nearestObjects [[worldSize/2, worldSize/2, 0], ['Land_Airport_Tower_F','Land_Hangar_F','Land_TentHangar_V1_F'], 50000]; }; private _safePos = if (count _airfields > 0) then { position (_airfields select 0) } else { [worldSize * 0.1, worldSize * 0.1, 0] }; ['RAVEN ACTUAL: All callsigns RTB to airfield for refit and resupply!'] remoteExec ['systemChat', 0]; { private _player = _x; private _tpPos = [(_safePos select 0) + (random 20) - 10, (_safePos select 1) + (random 20) - 10, 0]; _player setPos _tpPos; } forEach allPlayers; ['All players teleported to airfield safe zone.'] remoteExec ['systemChat', 0]; ['Location: ' + str _safePos] remoteExec ['systemChat', 0];",
  "description": "Teleport all players to airfield"
}
