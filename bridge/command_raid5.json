{
  "sqf": "private _fobPos = missionNamespace getVariable ['CLAUDE_FOB', getMarkerPos 'FOB RAVEN']; if (_fobPos isEqualTo [0,0,0]) then { { if ((toLower (markerText _x)) find 'raven' >= 0 || (toLower _x) find 'raven' >= 0) exitWith { _fobPos = getMarkerPos _x; }; } forEach allMapMarkers; }; ['[0235] TECHNICALS! Enemy vehicles approaching with heavy weapons!'] remoteExec ['systemChat', 0]; private _techDirs = [[160, 550], [200, 520], [70, 580]]; { private _dir = _x select 0; private _dist = _x select 1; private _spawnPos = [(_fobPos select 0) + (sin _dir) * _dist, (_fobPos select 1) + (cos _dir) * _dist, 0]; private _grp = createGroup EAST; private _tech = createVehicle ['CUP_O_LR_MG_TKM', _spawnPos, [], 0, 'NONE']; createVehicleCrew _tech; (crew _tech) joinSilent _grp; _grp setBehaviour 'COMBAT'; _grp setCombatMode 'RED'; private _wp = _grp addWaypoint [[(_fobPos select 0) + (sin _dir) * 150, (_fobPos select 1) + (cos _dir) * 150, 0], 20]; _wp setWaypointType 'SAD'; } forEach _techDirs; [''] remoteExec ['systemChat', 0]; ['═══════════════════════════════════════'] remoteExec ['systemChat', 0]; ['  TALIBAN NIGHT RAID IN PROGRESS'] remoteExec ['systemChat', 0]; ['  - Mortar teams firing from 700m south'] remoteExec ['systemChat', 0]; ['  - 6 assault teams (48 fighters) advancing'] remoteExec ['systemChat', 0]; ['  - 3 technicals with DShK'] remoteExec ['systemChat', 0]; ['  DEFEND THE FOB!'] remoteExec ['systemChat', 0]; ['═══════════════════════════════════════'] remoteExec ['systemChat', 0];",
  "description": "Taliban Night Raid - Technicals"
}
