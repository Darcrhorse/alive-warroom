{
  "sqf": "private _fobPos = getMarkerPos 'FOB RAVEN'; if (_fobPos isEqualTo [0,0,0]) then { { if ((toLower (markerText _x)) find 'raven' >= 0 || (toLower _x) find 'raven' >= 0) exitWith { _fobPos = getMarkerPos _x; }; } forEach allMapMarkers; }; if (_fobPos isEqualTo [0,0,0]) exitWith { ['ERROR: FOB RAVEN not found!'] remoteExec ['systemChat', 0]; }; missionNamespace setVariable ['CLAUDE_FOB', _fobPos, true]; ['═══════════════════════════════════════'] remoteExec ['systemChat', 0]; ['  FORTIFYING FOB RAVEN - USMC AIRBASE'] remoteExec ['systemChat', 0]; ['═══════════════════════════════════════'] remoteExec ['systemChat', 0]; private _respawn = createMarker ['respawn_west', _fobPos]; _respawn setMarkerType 'respawn_inf'; _respawn setMarkerColor 'ColorBLUFOR'; ['WEST Respawn point set at FOB RAVEN.'] remoteExec ['systemChat', 0]; private _hBarriers = []; for '_i' from 0 to 11 do { private _angle = _i * 30; private _pos = [(_fobPos select 0) + (sin _angle) * 80, (_fobPos select 1) + (cos _angle) * 80, 0]; private _barrier = createVehicle ['Land_HBarrier_Big_F', _pos, [], 0, 'NONE']; _barrier setDir (_angle + 90); _hBarriers pushBack _barrier; }; ['Perimeter barriers placed.'] remoteExec ['systemChat', 0];",
  "description": "FOB RAVEN - Perimeter & Respawn"
}
