{
  "sqf": "if (!isNil 'CLAUDE_virtualizeLoop') then { terminate CLAUDE_virtualizeLoop; }; CLAUDE_cacheRange = 2000; ['═══════════════════════════════════════'] remoteExec ['systemChat', 0]; ['  VIRTUALIZATION SYSTEM ONLINE'] remoteExec ['systemChat', 0]; ['  Cache Range: 2000m'] remoteExec ['systemChat', 0]; ['═══════════════════════════════════════'] remoteExec ['systemChat', 0]; CLAUDE_virtualizeLoop = [] spawn { while {true} do { private _players = allPlayers; private _allAI = allUnits select { !isPlayer _x && alive _x && !(vehicle _x isKindOf 'Air') }; { private _unit = _x; private _minDist = 999999; { _minDist = _minDist min (_unit distance _x); } forEach _players; if (_minDist > CLAUDE_cacheRange) then { if (simulationEnabled _unit) then { _unit enableSimulationGlobal false; _unit hideObjectGlobal true; }; } else { if (!simulationEnabled _unit) then { _unit enableSimulationGlobal true; _unit hideObjectGlobal false; }; }; } forEach _allAI; sleep 5; }; }; ['Virtualization active - distant units cached.'] remoteExec ['systemChat', 0];",
  "description": "Virtualization System"
}
