{
  "sqf": "['═══════════════════════════════════════'] remoteExec ['systemChat', 0]; ['BLUFOR TACTICAL OPERATIONS CENTER ONLINE'] remoteExec ['systemChat', 0]; ['Commander: CLAUDE - Callsign OVERLORD'] remoteExec ['systemChat', 0]; ['═══════════════════════════════════════'] remoteExec ['systemChat', 0]; private _bluforGroups = allGroups select {side _x == WEST && {alive (leader _x)} && {!(isPlayer (leader _x))}}; private _bluforCount = {side _x == WEST && alive _x} count allUnits; private _opforCount = {side _x == EAST && alive _x} count allUnits; ['SITREP:'] remoteExec ['systemChat', 0]; [format ['  BLUFOR Units: %1', _bluforCount]] remoteExec ['systemChat', 0]; [format ['  OPFOR Units: %1', _opforCount]] remoteExec ['systemChat', 0]; [format ['  BLUFOR Groups under command: %1', count _bluforGroups]] remoteExec ['systemChat', 0]; ['OVERLORD: All BLUFOR callsigns, report status. Preparing tactical assessment.'] remoteExec ['systemChat', 0];",
  "description": "BLUFOR Command - Initialize TOC"
}
